`
if uncompressed_length {
    `#define ` symbol `_UNCOMPRESSED_LENGTH ` uncompressed_length nl
}
`#define ` symbol `_LENGTH ` length nl
`#define ` symbol
with each line using # {
    ` \` nl
    `   "` $ `"`
}
`
