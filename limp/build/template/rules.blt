`
with each $ using # {
    if (comment) {
        with each explode(comment) using # {
            nl `# ` $
        }
    }

    nl `rule ` name

    with each vars using # {
       nl  `   ` name ` = ` value
    }
    nl
}
`
